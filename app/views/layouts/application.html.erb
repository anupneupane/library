<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Library</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="keywords" content="">
    <meta name="author" content="">
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%#= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <!-- HTML5 Support for IE -->
    <!--[if lt IE 9]>
    <script src="js/html5shim.js"></script>
    <![endif]-->
  </head>

  <body>
    <div id="modal-wrapper"></div>
    <%# render partial: 'shared_partials/login_modal' unless logged_in? %>
    <%# render partial: 'shared_partials/signup_modal' unless logged_in? %>
    <%# render partial: 'topics/form_modal', locals: {topic: Topic.new} if ( @channel && logged_in?)%>
    <%= render partial: 'layouts/menubar' %>

    <div class="content">
      <div class="container-fluid">
      <div class = "row-fluid table-wrap">
        <div class="sidebar2">

          <%= render partial: 'layouts/sidebar' %> 
        </div>
          

        <div class="main">
      <div class="main-content">
          <%= render partial: 'shared_partials/flash_messages' %>
          <%= yield %>
        </div>
        </div>
      </div>
    </div>
  </div>

  </body>
</html>
