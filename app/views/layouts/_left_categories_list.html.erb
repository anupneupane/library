<li class="dropdown-submenu"><%= link_to c.title, category_path(c) %>
  <ul class="dropdown-menu">
    <% c.topics.each do |t| %>
      <li><%= link_to t.title, topic_path(t) %></li>
    <% end %>
  </ul>
</li>